{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.EntityFrameworkCore": "Information"
      }
    },
    "WriteTo": [ { "Name": "Console" } ],
    "Enrich": [ "FromLogContext" ]
  },
  "ConnectionStrings": {
    "Postgres": ""
  },
  "Bootstrap": {
    "//": "Slice OPS.M.22.6 — Bicep populates this array per deploy. Empty here so local runs no-op. Prod cutover: add operator + team leads via infra/main.bicep before first deploy.",
    "SeedPlatformAdmins": []
  }
}
